# Risk register — reliability rollout

L = likelihood, I = impact (H/M/L). "Seen" = already happened during this work.

| ID | Risk | L | I | Seen | Detection | Mitigation |
|---|---|---|---|---|---|---|
| R01 | Textual merge conflicts between branches (`chat_screen.dart`, `active_chat_service.dart`, arb) | H | M | ✅ F6×F4 | `git merge` in the integration branch | Merge order in plan §2; resolve keeping the structural change and re-adding the small edit; `gen-l10n` + analyze right after. |
| R02 | **Semantic** interaction between individually correct fixes (F3 roster closes a turn, F4 hint-only watchdog, F10 activity model) — a turn shown as idle or working wrongly | M | H | — | interaction tests + device D-03/D-04/D-11 | One owner per decision (plan §3); tests that combine two fixes; after any change to one, re-run the other two's suites and the device scenarios. |
| R03 | Unit tests green but the behaviour is not applied on the device (wrong build installed, stale generated l10n, feature only on another path) | M | H | ✅ (stale build risk, harness bugs) | device matrix with captures + `versionCode` check after install | Always verify `dumpsys package … versionCode` after install; every fix has a device scenario; A/B vs pure main. |
| R04 | Flaky tests under machine load hide real failures / cry wolf | H | M | ✅ (4 random failures with 5 suites in parallel) | failures differ between runs | ONE full suite alone; re-run failures in isolation and on pure main before dismissing. |
| R05 | Reading-only harness limits (uiautomator unusable with Flutter) make device checks fragile | M | M | ✅ | scripts produce wrong taps | Seed prompts via SEND intent, fixed coordinates verified on a screenshot first, contact sheets; treat an invalid run as invalid (never as pass). |
| R06 | Behaviour change needing an owner decision: durable reasoning now visible (collapsed) although the old code hid it on purpose | M | M | — | review + device | Isolated render path; owner decides after seeing it on the Pixel; flag stays reversible. |
| R07 | Reaping a live turn wrongly (F3): a server returning a partial roster closes a running turn | L | H | — | tests for incomplete/malformed roster; device D-03 | Only complete, well-formed rosters count; 2 consecutive absences; runtime must have been seen live; pre-start exemption bounded to 15 s. |
| R08 | Background subagent state goes stale in the other direction (never completes) if refresh stops at turn end | M | M | — | test "listed after turn end stays working" + D-11 | F10 owns the polling rules with bounded cadence and hysteresis. |
| R09 | Pre-existing bugs blamed on the new code (or vice-versa) | M | M | ✅ F8, F10 were pre-existing | — | A/B against pure 1.2.11 build (gate G3) before attributing. |
| R10 | Two writers in one worktree (orphan Hermes/Desktop sessions edited the repo) | M | H | ✅ | mtime changes not made by us | One writer per worktree; check `state.db` recent messages/`agent.log` before writing; user restarts the dashboard with `! systemctl --user restart hermes-dashboard.service` when needed. |
| R11 | Usage limits interrupt agents mid-task (Opus fan-out hit the session limit) | M | M | ✅ | HTTP 429 | GPT workers via `claude-multi` for code, Sonnet for design, Opus only for something very specific; ≤3 parallel agents; save state in worktrees. |
| R12 | Format-only diff noise inflates PRs and conflicts | M | L | ✅ (Sol, watchdog) | script: hunks equal after stripping whitespace/commas/braces | Ask agents to format only added lines; run the noise script before commit — and re-verify after (a brace-stripping heuristic once broke a file). |
| R13 | Android/OEM variance for background work and notifications (Doze, app standby, foreground-service limits, Android 14+ rules) | H | H | — | cross-device research + physical tests on phone AND tablet | Design around what the OS guarantees; document what a force-stopped app can never receive; catch-up on next launch. |
| R14 | Data safety on the owner's phone (uninstall = lose pairing/data) | L | H | — | — | Never uninstall; same signing key; higher build number; keep the QA package separate from production. |
| R15 | Backend evolves (upstream 184 commits ahead): events/columns Console relies on change | M | M | ✅ (`codex_message_items` is new) | compare against upstream before release | Re-check Desktop reference against upstream; prefer structured fields; keep tolerant parsers. |
| R16 | Security: media/file delivery could fetch or render arbitrary host paths | M | H | — | security review of F12 | Use the same authorization Desktop uses; size limits; never fetch arbitrary paths from text heuristics; no secrets in logs. |
| R17 | Release/sign mistakes (wrong key, uploading an unverified build) | L | H | — | release gates | Signed builds only via the release scripts; owner's go; do not read `key.properties`. |
| R18 | Scope creep across ~14 items stalls everything | H | M | ✅ | this file | Strict order in plan §2; each item closes with evidence before the next starts; new asks go to the backlog table in spec §3. |
| R19 | Reopening the app mid-turn leaves the chat duplicated/stuck forever (pre-existing in 1.2.11); our removal of the 90/240 s watchdog means nothing ends it any more | H | H | ✅ measured (D-12) | device D-12/D-12b, A/B | Fix F14 (Sol): single user bubble structurally, convergence to the durable transcript when the roster no longer lists the runtime or the resume never binds. **Blocking gate: no PR of F3/F4 before D-12 passes.** |
| R20 | A fix passes ITS targeted tests but breaks tests in a file nobody ran (F9 broke two privacy tests in `active_chat_resume_snapshot_test.dart`); and a fix can silently reverse a deliberate privacy contract | M | H | ✅ F9 | full suite on the combined branch; per-branch "privacy/contract" suites | Every agent prompt lists the contract suites it MUST run (`active_chat_resume_snapshot_test`, `transcript_display_privacy_test`, `session_reconciler_test`, `chat_render_projection_test`); the orchestrator re-runs them on the combined branch after every merge. F9 is split: F9a (reply text from `codex_message_items`, privacy-neutral) proceeds; F9b (show reasoning, reverses "mobile never carries reasoning") is PARKED in `fix/console-reasoning-and-sidecar-parity` until the OWNER decides. |
